export enum Scoring {
    Cvss3 = 0,
    RiskMatrix = 1
}

export enum RessourceName {
    Engagement = 'engagement',
    Contact = 'contact',
    Company = 'companie'
}

export enum ApiRessource {
    Engagement = 'engagements',
    Contact = 'contacts',
    Company = 'companies'
}
